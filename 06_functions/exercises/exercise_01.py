# Глава 06 — Упражнение 1
# Напишите функцию format_policy_card(client_dict),
# которая принимает словарь с данными клиента и
# возвращает отформатированную строку-карточку.

def format_policy_card(client):
    """
    client: dict с ключами: name, age, car, premium, policy_number
    Возвращает: многострочную строку с карточкой полиса
    
    Пример вывода:
    ──────────────────────────────────────
      СТРАХОВОЙ ПОЛИС — ПолисПлюс
    ──────────────────────────────────────
      ФИО:    Волков Денис Андреевич
      Возраст:41 лет
      Авто:   Kia Sportage
      Полис:  POL-2024-00157
      Премия: 18,400 руб./год
    ──────────────────────────────────────
    """
    sep = "─" * 38
    # Ваш код — замените ... на нужные значения:
    return (
        f"{sep}\n"
        f"  СТРАХОВОЙ ПОЛИС — ПолисПлюс\n"
        f"{sep}\n"
        f"  ФИО:    {client['name']}\n"
        f"  Возраст:{client['age']} лет\n"
        f"  Авто:   {client['car']}\n"           # ← что сюда поставить?
        f"  Полис:  {client['policy_number']}\n"  # ← и сюда?
        f"  Премия: {client['premium']:,} руб./год\n"
        f"{sep}"
    )

# Тест — запустите и посмотрите что получается:
test_client = {
    "name": "Волков Денис Андреевич",
    "age": 41,
    "car": "Kia Sportage",
    "premium": 18_400,
    "policy_number": "POL-2024-00157"
}

print(format_policy_card(test_client))

# Дополнительно: сделайте так чтобы функция работала и без поля "car"
# (если "car" нет в словаре — выводите "не указан")
